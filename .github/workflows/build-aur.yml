name: Build and Test AUR Package

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      - name: Checkout code
        uses: actions/checkout@v3 # Ensure we're using the latest version
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2 # Ensure we're using the latest version

      - name: Build AUR Package
        run: |
          docker run --rm -v ${{ github.workspace }}:/workspace -w /workspace archlinux:base-devel /bin/bash -c "
          pacman -Sy --noconfirm fakeroot wget sudo
          useradd -m -r aurbuilder
          mkdir -p /etc/sudoers.d
          echo 'aurbuilder ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/aurbuilder
          chown -R aurbuilder /workspace
          sudo -u aurbuilder bash -c 'cd /workspace && wget https://github.com/cosmic-utils/gui-scale-applet/archive/refs/tags/2.0.0.tar.gz -O gui-scale-applet-2.0.0.tar.gz && makepkg -si --noconfirm'"
        shell: bash
      
      - name: Test AUR Package
        run: |
          docker run --rm -v ${{ github.workspace }}:/workspace -w /workspace archlinux:base-devel /bin/bash -c "
          sudo -u aurbuilder bash -c 'cd /workspace && makepkg --noconfirm'"
        shell: bash
